# MVP 验收记录 · 2026-09-08

- ASS 有效双语台词：1,637 条，唯一来源为素材目录 flipped.ass。
- Python 自动测试：3 passed。覆盖真实素材、短台词、逗号、ASS 样式清理、中英包含搜索、大小写、空结果、分页参数与非法片段 ID。测试库有两条弃用提示，不影响执行。
- React/Vite 生产构建通过，npm 安装审计 0 vulnerabilities。
- 三个实际切片：字幕 1（49.24–52.47 秒），819（2550.67–2551.83 秒），1628（5016.40–5018.63 秒）。H.264/AAC，实际时长分别 3.25325、1.167833、2.25225 秒，误差均小于 0.024 秒。
- 逐张查看三段中点截图，中英字幕均完整可见，文案与索引相符。未逐句人工核听整部影片；不改变原 ASS 同步偏移。
- 浏览器实测：leave me alone 4 句；喜欢 23 句；beautiful 4 句。中文高亮正确。不存在的台词显示无结果提示。
- 实际播放器播放至结束：currentTime = duration = 3.25325；点击 Replay 后 currentTime = 0、paused = false、ended = false。
- 搜索切换后旧播放器移除；索引分页前 30 条与第二页 30 条不重复。
- 视频 Range 请求返回 206，bytes=0-1023 返回 1024 字节。
- 独立代码审查发现首次 pip 下载失败后启动无法恢复，已改为每次启动检查依赖存在，缺失则补装。最终 PowerShell 脚本语法检查通过。

具体转码证据位于 `data/verification/report.json` 及三张 cue 截图。

## 视频网格调整

- 新增真实预览图生成和缓存接口；自动测试 4 passed，生产构建通过。
- 1440px 桌面验证 3 列、所有视频初始 paused=true，无自动播放。
- 点击首张后原地播放；重播首张后点击第二张，首张暂停在 0.247572 秒、第二张从 0 秒播放，未发生叠音。
- 390px 手机验证单列，无横向溢出。
